import { FC } from 'react';
import { TimelinePresenter } from './TimelinePresenter';

type TimelineContainerProps = {};

export const TimelineContainer: FC<TimelineContainerProps> = (props) => {
  const {} = props;
  return <TimelinePresenter />;
};
